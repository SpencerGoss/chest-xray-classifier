# App theme — matches the presentation deck (teal + amber, clean medical look).
# This copy makes the cxr_app/ folder self-contained if run from inside it.
[theme]
primaryColor = "#10495C"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#EEF3F4"
textColor = "#222A30"
font = "sans serif"

[browser]
gatherUsageStats = false
